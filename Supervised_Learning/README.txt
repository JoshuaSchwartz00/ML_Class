Libraries used: scikit-learn, matplotlib, pandas, numpy

1. Clone the github repository with: "git clone https://github.com/JoshuaSchwartz00/ML_Class.git"
2. Access the ML_Class/Supervised_Learning directory
3. Run the code within by inputting: "py -3 supervised_learning.py"

The graphs will automatically save as pngs in the Supervised_Learning directory.

Analysis is included in the report.
